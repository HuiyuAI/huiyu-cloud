spring:
  main:
    allow-circular-references: true
  profiles:
    active: dev
  application:
    name: huiyu-service
  cloud:
    nacos:
      discovery:
        server-addr: 101.43.14.157:3333
      config:
        server-addr: 101.43.14.157:3333
        # 默认命名空间public 如果加上会重复打印日志
#        namespace: public
        group: DEFAULT_GROUP
        prefix: huiyu-service
        file-extension: yml